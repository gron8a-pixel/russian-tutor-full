{
  "scripts": {
    "build": "cd client && npm ci && npm run build && cd ../server && rm -rf public && mv ../client/dist public",
    "start": "cd server && node index.js"
  },
  "devDependencies": {
    "cpy-cli": "^6.0.0"
  }
}

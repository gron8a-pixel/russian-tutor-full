{
  "name": "russian-tutor-full",
  "version": "1.0.0",
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "build": "cd client && npm ci && npm run build",
    "start": "cd server && node index.js"
  }
}
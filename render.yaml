services:
  - type: web
    name: russian-tutor
    env: node
    region: frankfurt # или ближайший
    buildCommand: |
      npm install
      npm run build
    startCommand: npm run start --workspace=server
    envVars:
      - key: NODE_VERSION
        value: "18"